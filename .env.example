# Copy these into Vercel Project Settings > Environment Variables
# Do NOT commit real values to the repo

# Canva OAuth
CANVA_CLIENT_ID=
CANVA_CLIENT_SECRET=

# Redirect URIs
# Local dev redirect used by /auth
CANVA_REDIRECT_URI=http://127.0.0.1:4000/callback
# Production redirect for Vercel (must be whitelisted in Canva Dev Console)
CANVA_REDIRECT_URI_PROD=https://<your-vercel-project>.vercel.app/callback

# Canva API base (override only for testing)
CANVA_API_BASE=https://api.canva.com

# Optional: provide access token via env instead of tokens.json storage
CANVA_ACCESS_TOKEN=

# General
NODE_ENV=production
PORT=4000
