import { z } from 'zod';
export const zodJsonSchema = z.lazy(() => {
    return z.union([
        z.string(),
        z.number(),
        z.boolean(),
        z.null(),
        z.array(zodJsonSchema),
        z.record(z.string(), zodJsonSchema),
    ]);
});
// Shared schema for paginated responses
export const PaginatedResponseSchema = (dataSchema) => z.object({
    data: z.array(dataSchema),
    cursor: z.string().nullable(),
    hasMore: z.boolean(),
});
//# sourceMappingURL=shared.js.map